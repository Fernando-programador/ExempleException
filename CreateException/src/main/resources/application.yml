server:
  port: 8085
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my_exception?useSSL=false
    username: root
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update 
      
